# PRODUCTION — copy to Vercel → Settings → Environment Variables, then redeploy
# Base URL must include /api (Express mounts routes under /api)

VITE_API_URL=https://YOUR-BACKEND-PUBLIC-URL.up.railway.app/api
VITE_WS_URL=wss://YOUR-BACKEND-PUBLIC-URL.up.railway.app
VITE_AI_AGENT_URL=https://YOUR-AI-AGENT-PUBLIC-URL.up.railway.app
